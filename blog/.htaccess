RewriteEngine On

# Evitar interferir con archivos reales
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir slug a post.php
RewriteRule ^([^/]+)/?$ post.php?slug=$1 [L,QSA]